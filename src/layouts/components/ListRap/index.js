import classNames from 'classnames';

const cx = classNames;
function ListRap() {
    return (
        <div className={cx('justify-center ml-80 pt-8')}>
            <p className={cx('text-lq-white text-4xl')}>L&Q Cineplex Quang Trung</p>
        </div>
    );
}

export default ListRap;
